#!/usr/bin/env bash

set -e

echo "===================================="
echo " Recovery Patcher 2026"
echo "===================================="

WORK_DIR="work"
OUTPUT_DIR="output"
LOG_DIR="logs"
TOOLS_DIR="files"

mkdir -p "$WORK_DIR"
mkdir -p "$OUTPUT_DIR"
mkdir -p "$LOG_DIR"

echo "[OK] Directorios creados"
